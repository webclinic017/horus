// struct SampleStrategy {}

// impl Strategy for SampleStrategy {

// }
fn main() {

    // let order_routing = vec![
    //     binance_router.get_market_by_symbol("BTCEUR");
    // ];

    // let strategy = SampleStrategy::new(order_routing);

    // strategy.run().join.unwrap();
}